# Copy to .env and fill values as needed
COINGECKO_API_BASE=https://api.coingecko.com/api/v3
# Optional: If you have a pro key (not required for free endpoints)
COINGECKO_API_KEY=
# Comma separated extra symbols to track that may not be in the Excel snapshot
EXTRA_SYMBOLS=
# Set your base fiat currency (gbp or usd)
BASE_FIAT=gbp
REFRESH_SECONDS=120
